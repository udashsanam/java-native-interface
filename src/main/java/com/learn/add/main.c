#include <stdio.h>
#include "add.h"  // Include the header file for the function

int main() {
    int result = add(5, 3);  // Directly call the add function
    printf("Result of addition: %d\n", result);
    return 0;
}
